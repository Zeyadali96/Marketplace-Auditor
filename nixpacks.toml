# Nixpacks configuration for Playwright dependencies
[phases.setup]
nixPkgs = ["playwright-driver.browsers", "glib", "nss", "nspr", "atk", "at-spi2-atk", "libdrm", "expat", "libxcb", "libxkbcommon", "mesa", "alsa-lib"]

[phases.build]
dependsOn = ["setup"]
cmds = ["npm install", "npx playwright install chromium", "npm run build"]
